{
  "name": "luxe-beauty-lounge",
  "version": "1.0.0",
  "description": "Luxe Beauty Lounge — salon website",
  "scripts": {
    "dev": "npx live-server --port=3000",
    "start": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
